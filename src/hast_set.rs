use std::collections::HashSet;
mod add_mod;

use crate::add_mod::config;

fn main() {
    // HashSet
    // let mut my_set: HashSet<i32> = HashSet::new();

    // my_set.insert(23);

    // let sum = config::add(1, 3);
    // println!("{}", sum);
}
