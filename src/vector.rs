fn main() {
    // Vector
    let mut v: Vec<i32> = Vec::new();

    v.push(32);

    v.push(45);

    println!("{:?}", v);

    // let mut v: Vec<i32> = vec![1, 2, 3, 4];

    // v.push(5);
    // println!("{:?}", v);

    // v.remove(1);
    // println!("{:?}", v);

    // println!("{:?}", v.get(1).unwrap());

    // println!("for loop in vector");

    // for val in v {
    //     println!("{}", val);
    // }

    // let arg = 12;
    // let age = Box::new(21);

    // println!("{} and age is {}", arg, age);
}
